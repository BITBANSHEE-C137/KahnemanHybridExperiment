[Unit]
Description=ttyd - Terminal over WebSocket
After=network.target

[Service]
Type=simple
User=claude-operator
ExecStart=/usr/local/bin/ttyd --port 7681 --interface lo --writable --base-path /terminal bash --login
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
